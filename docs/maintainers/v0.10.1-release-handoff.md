# v0.10.1 Patch Release Handoff

**Audience**: Operator and maintainers.\
**Prepared**: 2026-09-15.\
**Status**: Fully published and verified; release run green.

## Candidate and Published Identity

S152 prepared 0.10.1 across all ten product crates, candidate output goldens, portable conformance metadata, package certification and short release highlights. Following human merge of PR #417 and separate explicit owner publication authorization, S153 verifies actual publication of the merged patch. It contains S151 Doctor diagnostics: discoverable timings, nested readiness boundaries, one-second pending progress and automatic slow-completion durations, without changing final reports, facts or repair authority. The [short highlights](../../release-notes/v0.10.1.md) summarize the patch and link to its complete tagged changelog.

Actual publication is [v0.10.1](https://github.com/h8rt3rmin8r/fragcap/releases/tag/v0.10.1) from exact merged source `a7d24962999d38d7ff130722859d473543864862`, verified through the [official release run](https://github.com/h8rt3rmin8r/fragcap/actions/runs/35033304343). Historical v0.10.0 from `787739edfa8d748e25cb4b5c4965f5c936850d16`, its [immutable handoff](v0.10.0-release-handoff.md), tags, certified assets, registry versions and source history are unchanged. The candidate's applicability and changelog preparation date did not prove publication; the [publication record](../published-release.json) and fourteen current markers change only after complete public verification. Packages retain the existing unsigned policy; checksums and certification do not imply signing.

## 2026-09-15: Actual Publication Verification

Annotated tag object `61796e9a09abcbbc2642c36f4805868bf0c8d546` peels to merged S152 source `a7d24962999d38d7ff130722859d473543864862`. All nine applicable merged-source workflows passed before tagging. Release run 35033304343 passes all four required jobs: identity, final Windows package certification, GitHub release creation and registry publication. No rerun, failed boundary, tag retarget, asset substitution or gate change was required. The public non-draft, non-prerelease release became live at `2026-09-15T23:08:58Z`; registry publication completed successfully at `2026-09-15T23:38:00Z`.

The publish job was held behind the existing owner gate. GitHub's run approval history records User `h8rt3rmin8r` (id 46768484), state approved, for `crates-io` (environment 21294112472). This was actual normal owner approval, not agent approval or administrator bypass. Administrator bypass remains enabled and unchanged; owner approval is a separate action, not independent two-person review. The publish job's fresh protection check passes before execution.

Exact-run certification summary artifact 10422716402 and certified distribution artifact 10422128330 bind provenance. The downloaded report has schema 2, official version 0.10.1, exact peeled-tag source, target x86_64-pc-windows-msvc, fragcap-native backend and exact etw/live/native-deep-capture/socket-table features. It is complete with zero findings and six complete artifact rows. Canonical report validation plus independent downloaded-file size/hash and three checksum-sidecar checks pass without unpacking, installing or executing the product. The [slice verification record](../../specs/153-release-publication/verification.md) retains all six hashes and registry identities.

| Final asset | Bytes | SHA-256 |
| --- | --- | --- |
| `fragcap-0.10.1-x86_64-pc-windows-msvc.zip` | 6525562 | `652882bea0705b397f6a1a012fce828f0f808635f993e1d133902e0642f5e786` |
| `fragcap-0.10.1-x86_64.msi` | 6823936 | `309b4cf467bf29521524065a807002843b2b97274f3d4036b0f9a8af1b374356` |
| `catalog.db` | 90112 | `d1da331be8e1c33b58f53b35e6a331755bfbc315d75be811214cc83488104fff` |

All ten public registry version records independently report exact 0.10.1, yanked=false and an archive checksum: fragcap-core, fragcap-profile, fragcap-capture, fragcap-attr, fragcap-sink, fragcap-steam, fragcap-targets, fragcap-proxy, fragcap and fragcap-cli. Download matching sidecars from the official release before operator-owned use. Successful release publication does not merge the later records PR or complete independent installed-build acceptance.

## Registry Approval Protection

The existing `crates-io` environment (id 21294112472) normally requires User `h8rt3rmin8r` (id 46768484) review, allows self-review because that account also initiates release workflows, explicitly retains administrator bypass at the operator's direction and permits exactly one custom tag rule `v*` (id 60078197), with no branch allowance. Required review is the normal path, but the owner retains the ability to deliberately bypass it; record any such later action as bypass, not reviewer approval. This is owner-controlled separate action, not independent two-person review. S152 verified effective protection through fresh authoritative public API records on 2026-09-15; future setting changes invalidate that evidence. No deployment was approved or bypassed during preparation.

The [release workflow](../../.github/workflows/release.yml) checks exact tag/workspace agreement and freshly checks protection before certification. The release job independently re-fetches immediately before GitHub release creation, so certification time does not make that check stale. After normal owner environment approval or a deliberate owner bypass, publication re-fetches and checks policy immediately before registry execution. A missing environment, unknown or weakened protection, unavailable metadata or incomplete allowance inventory refuses. Environment existence or successful API mutation alone never counts as verified approval protection.

## Reproducible Verification

Use temporary files outside tracked source and fetch both public records immediately before checking them:

```sh
gh api --method GET repos/h8rt3rmin8r/fragcap/environments/crates-io > environment.json
gh api --method GET 'repos/h8rt3rmin8r/fragcap/environments/crates-io/deployment-branch-policies?per_page=100' > policies.json
cargo xtask release-guard environment.json policies.json
```

Use a shell that stops on failed fetches; the workflow uses `set -euo pipefail`. Each document is bounded to 1 MiB and the complete inventory must contain only the approved tag rule. `cargo xtask release-guard` alone checks repository wiring offline, not live settings. Success with supplied current records verifies configuration, never the operator's actual deployment approval.

Authenticated workflow reads use explicit read-only Actions permission. Identity and publish inherit it from the workflow default; release repeats it in its existing Contents write override because job permission maps replace defaults. The offline guard rejects missing effective read authority without adding credentials, write scopes or restrictions on the operator.

If protection needs repair, an authorized maintainer must reread the existing environment, preserve identity, secrets, variables, timers and unrelated settings, configure the approved reviewer/custom policy and verify readback. GitHub's [REST environment documentation](https://docs.github.com/en/rest/deployments/environments) does not currently expose the administrator-bypass mutation in its request schema. S152's bounded requests with that attribute succeeded and fresh readback confirmed it; never assume that undocumented behavior remains supported. In [environment settings](https://docs.github.com/en/actions/how-tos/deploy/configure-and-manage-deployments/manage-environments), preserve the checked “Allow administrators to bypass configured protection rules” choice because the operator explicitly requires it. Do not approve or bypass pending deployment work as an agent, disable owner bypass, or weaken the validator to make repair appear successful.

## Remaining Operator Actions

S152 PR #417 is merged, and the subsequent explicit release request has been fulfilled by verified v0.10.1 publication. S153 reconciles actual identity and current baseline markers through a separate bounded human-merge records PR; review that PR after its own current-head CI and all received bot reviews pass. It does not alter the already published tag, source or bytes. No additional owner deployment action is required for this completed release.

Operator field validation uses newly published bytes. Agents do not install or execute the sensitive application, production Doctor or real games on the operator machine. Actual elevated first-run/repeat measurements remain [#372](https://github.com/h8rt3rmin8r/fragcap/issues/372); independent audit [#333](https://github.com/h8rt3rmin8r/fragcap/issues/333), installed retest [#413](https://github.com/h8rt3rmin8r/fragcap/issues/413), documentation reconciliation #331 and final #334/#278 remain open. Deferred #155/#94 are unchanged. Bot review and controlled CI are not substitutes for independent product acceptance.
