# Documentation site build and deploy. Specification 22, 24.2.
#
# SKELETON. Owned by slice S18. There is no site to build yet, so this
# declares its trigger and purpose and does nothing else. It deliberately does
# not report success for work it has not done.
name: docs

on:
  push:
    branches: [main]

jobs:
  build:
    name: build and deploy the documentation site
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Not yet implemented
        run: |
          echo "The documentation site is owned by slice S18."
          echo "This workflow is a declared skeleton, not a passing build."
          exit 1
