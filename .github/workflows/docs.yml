# Documentation site build and deploy. Specification 22, 24.2.
#
# SKELETON. Owned by slice S18. There is no site to build yet.
#
# Manual dispatch only, deliberately. A skeleton that runs automatically and
# fails reports red on every push forever, which is as uninformative as
# reporting green would be and trains readers to ignore the signal. Not
# reporting is honest; reporting false success is not. S18 adds the real
# triggers along with the site.
name: docs

on:
  workflow_dispatch:

jobs:
  build:
    name: build and deploy the documentation site
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Not yet implemented
        run: |
          echo "The documentation site is owned by slice S18."
          echo "This workflow is a declared skeleton, not a passing build."
          exit 1
