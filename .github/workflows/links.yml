# External reference verification. Specification 22.5, 24.2.
#
# SKELETON. Owned by slice S18, which builds the documentation this checks.
#
# Manual dispatch only until then. The weekly schedule belongs here eventually,
# because external link liveness is not a property of a change, but a scheduled
# job that fails every Monday is noise rather than signal. S18 restores the
# schedule when there is something to verify.
name: links

on:
  workflow_dispatch:

jobs:
  verify:
    name: verify external references
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Not yet implemented
        run: |
          echo "External link verification is owned by slice S18."
          echo "This workflow is a declared skeleton, not a passing check."
          exit 1
