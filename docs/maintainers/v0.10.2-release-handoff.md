# v0.10.2 Patch Release Handoff

**Audience**: Operator and maintainers.\
**Prepared**: 2026-09-20.\
**Status**: Candidate preparation only; not published.

## Candidate and Published Identity

S159 prepares 0.10.2 across all ten product crates, generated output goldens, portable conformance metadata, package certification and short release highlights. It includes merged S154 through S158 work: deterministic stalled-consumer testing, independent-review intake, deterministic package loopback authority, the complete native documentation contract, and Windows writer-start and observation-drain corrections.

Actual publication remains [v0.10.1](https://github.com/h8rt3rmin8r/fragcap/releases/tag/v0.10.1) from `a7d24962999d38d7ff130722859d473543864862`. Its immutable handoff, tags, six certified public files, registry versions and source history remain unchanged. Candidate applicability and a dated changelog do not prove publication. `docs/published-release.json` and the current review candidate remain v0.10.1 until actual v0.10.2 publication is verified.

## Publication Sequence

The owner reviews and merges the official S159 candidate pull request after current-head checks and received reviews are satisfied. The explicit release request authorizes the agent to create and push v0.10.2 from that exact merged source. The tag-triggered workflow rechecks version identity and registry protection, certifies Windows files, creates the GitHub release and then waits at the protected `crates-io` environment if owner approval is required.

The agent does not approve or bypass the protected deployment. After that owner action, the workflow rechecks policy and publishes all ten crates in dependency order. Publication is complete only after all four jobs are green, six public files and their checksums reconcile, and all ten registry records report exact 0.10.2 as non-yanked.

## Scope Boundaries

This patch does not claim independent security review, real-game compatibility, Doctor field validation or final Deep Capture completion. Issues #333, #372, #331, #334 and #278 retain those outcomes. Controlled source tests and disposable hosted Windows certification do not substitute for independent acceptance.

Agents do not install or execute the sensitive application, production Doctor, a real game, real trust mutation or sensitive live capture on the operator machine. Packages remain unsigned; checksum and certification evidence does not imply signing.

## Post-publication Reconciliation

After public verification, S159 creates a separate records-only pull request that binds the exact tag, merged source, workflow, owner approval, public files and registry identities. Current publication markers move only in that later human-reviewed change. The records pull request cannot alter already published bytes or their source.
