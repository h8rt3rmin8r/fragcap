`fragcap doctor` now lists the real capture-capable network interfaces instead
of always reporting that none were found; the empty-set warning appears only when
enumeration genuinely finds no interfaces.
