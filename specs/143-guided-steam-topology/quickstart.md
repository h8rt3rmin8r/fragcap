# Quickstart: Guided Steam Client Setup

## Focused Validation

```powershell
cargo test -p fragcap-targets --lib store --locked
cargo test -p fragcap-cli --test cli_calibrate --locked
cargo test -p fragcap-cli --test cli_reference --locked
cargo test -p fragcap-cli --lib events --locked
```

Confirm absent-only eligibility, positive canonical Steam anchors, exact application-id and install-root joins, unsuitable executable refusal, complete plan rendering, plan field sensitivity, human default decline, exact structured input, target and discovery revalidation, immediate conditional-update races, non-launch field preservation, authored fidelity, durable-id handoff, separate sequential registration/setup/session responses, and unchanged S139-S142 behavior.

## Repository Gate

```powershell
cargo xtask ci
```

## Safety Inspection

- Appinfo remains explicitly labeled proposal evidence and is never called an observed socket holder.
- Any present launch declaration is preserved byte-for-value.
- No target field changes before positive confirmation and fresh authority revalidation.
- The store update changes only launch declaration and fidelity under an exact expected-row condition.
- Setup authorization never authorizes trust, proxy, routing, launch, capture, artifact, or compatibility-fact effects.
- Registration, setup, and session plans consume separate input lines.
- No process handle, process control, topology observation, hidden trust, system proxy, pinning bypass, target key extraction, workflow storage, schema migration, dependency package, or second calibration session is added.
- Issue #400 closes on merge while parent #380 remains open.
