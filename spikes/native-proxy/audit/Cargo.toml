[package]
name = "fragcap-native-proxy-audit"
version = "0.0.0"
edition = "2021"
rust-version = "1.82"
publish = false
license = "Apache-2.0"

[workspace]

[dependencies]
hudsucker = { version = "=0.23.0", default-features = false, features = [
    "decoder",
    "http2",
    "native-tls-client",
    "rcgen-ca",
] }
