# S150 acceptance evidence

Date: 2026-09-15. Candidate: prepared v0.10.0 on release/0.10.0. This record reports controlled engineering evidence and release readiness, not independent #333 acceptance, operator installed-build validation, publication or final #334 completion.

## Chronological execution

1. Verified merged S149/source remote, created spec-kit specification, clarify/checklist, plan/research, tasks and cross-artifact analysis. Eleven requirements and six success criteria map to seventeen tasks without unresolved blocking ambiguity. Both requirement checklists passed before implementation.
2. Expanded parser-only current documentation corpus tests. The baseline failed on README's retired Deep Capture consent input and Mermaid node false positives. Added explicit Mermaid fence regression and corrected obsolete guidance. Default and net CLI-reference variants each passed all eight tests without command dispatch.
3. Added static twelve-area handoff tests before implementation. Baseline failed for missing committed registry and unimplemented negative validation. Final focused tests passed all six cases, including closed scope, source confinement, actual non-ignored test references and refusal of invented audit approval/provenance. Reused the existing threat-model test-source checker.
4. Corrected native architecture, first-session/calibration journey, compatibility applicability, CLI semantics and artifact authority. Added discoverable Doctor/troubleshooting, security/privacy, packaging/migration and stable API pages. The first manifest filter selected zero tests with the feature disabled; the corrected `cargo test -p fragcap --lib --features deep-capture --locked published_examples_match_the_versioned_reader` selected and passed the actual reader test. Published schema/examples are reused rather than inventing shortened artifacts.
5. Linked #411 into the official Project as S150/In progress, and posted bounded delivery references to #278/#331/#333. All parent independent/final criteria remain open. Doctor #372 remains operator reproduction on newly published bytes.
6. Release preflight caught unsupported historical `.feature.md` fragment suffixes from S130/S131/S132. Renamed them to `.added.md` with identical contents, retaining unknown-section refusal. Corrected S150's leading spec-impact comments. Documentation lint, specification check and changelog preview passed. Short-note validation correctly waited for the new release section.
7. Committed documentation preparation, previewed aggregate Cargo release without execution, then selected the structurally version-only subcommand because the aggregate banner still said Publishing with disabled publication. Previewed and executed `cargo release version 0.10.0 --workspace --config release.toml` (execute/no-confirm only on execution). Only local first-party manifest/lock versions changed. Offline workspace updates reconciled fuzz/performance locks without upgrading third-party packages.
8. Regenerated the existing three golden owners: facade goldens (7 tests), CLI Capture (24 tests), CLI extcap (19 tests), all passed. Updated exact embedded and portable conformance/staged-binary assertions, never historical physical measurements. Fresh graph snapshot retained package/edge counts 187/448 Linux, 186/424 Windows and 148/317 official release. Rebound the existing unsafe inventory only for first-party version metadata, without changing scope, expiry or exceptions.
9. The existing release task assembled 44 fragments into [0.10.0] dated 2026-09-15 and normalized historical Markdown wrapping. Consumed fragments are recoverable from Git history and represented in CHANGELOG.md. Its git-rm attempt could not stage the locally modified S150 decision file; targeted staging handles that deletion, not forced destructive removal. One-screen notes passed after assembly and do not copy the full changelog.
10. Initial full local CI passed Rust tests/clippy but rejected the stale unsafe graph digest. After the exact metadata reconciliation, the full gate was restarted. The declared MSRV 1.88 workspace build passed. Final aggregate evidence is recorded below when completion is observed.
11. Production site build and all four changelog unit tests passed. Browser tests initially rejected a stale fixed route count and TLS key-log search ranking. Route coverage now compares complete content inventory to exported routes, not a newly guessed count. All 69 routes passed 320/768/1440px sweeps, and current native guidance internal destinations/anchors passed. Added a bounded canonical key-log query pin to the authoritative output/sensitivity reference; focused search (including the new Library/Doctor pages) passed. Final full browser evidence is recorded below when completion is observed.

## Final local gates

`cargo xtask ci` completed with exit 0 after metadata reconciliation: fmt, clippy, workspace tests/doc-tests, lint, crate direction/license, closed supply-chain policy, package contract, wrappers/skills, default/net documentation checks, specification currency, guided calibration acceptance, threat model, review readiness, fuzz/failure inventories, portable HTTP/TLS conformance, performance authority and retained Windows evidence all passed. The declared MSRV 1.88 build completed with exit 0. TShark and actual final-package installer execution remain distinct hosted CI tiers; static local contract success does not relabel them as local installed-build proof. The final production build passed, all four changelog unit tests passed, and all thirteen browser tests passed (69 complete exported routes, three viewports, named diagrams, contrast, retired/current searches, native guide links/anchors and not-found recovery). Post-final-edit documentation default/net checks, notes and lint also completed with exit 0. Strict UTF-8/no-BOM and mojibake sanity checks plus git diff whitespace checks passed.

## External review and delivery

[PR #412](https://github.com/h8rt3rmin8r/fragcap/pull/412) was automatically published against main from release/0.10.0. The automatic first Codex review of adb7204 reported one P2 finding: the quickstart manifest command omitted the required feature and could select zero tests. Corrected it in 8f2f199 to the actual feature-enabled reader invocation and reran the exact documented command: one matching test passed, zero ignored. Every hosted CI tier passed on that revision, including actual final Windows package certification.

The single authorized manual second round was requested with `@Codex security review`; GitHub reports its completed activity as Code Review on 8f2f199, not a separate whole-product audit. It reported four P2 findings: enclosing test configuration, tracked Cargo ownership, workspace-version binding, and a misplaced specification-history row. Replaced text-only evidence admission with Git-tracked Cargo target ownership, explicit package-feature arrays and actual compiled harness discovery under those features with defaults disabled. The command builds targets and invokes only `--list`, not tests or the product. The not-started record now compares against the manifest's actual workspace version. Moved the revision row into the contiguous chronological history table and restored the product-description sentence.

Regression tests were added before implementation; the initial focused build failed on missing validation interfaces. All eleven focused tests now pass, including actual file-level feature omission, actual enclosing `cfg(any())` harness omission, tracked/non-target rejection, and future version mismatch. Focused Clippy and standalone real harness discovery pass. The initial recursive full-discovery unit test could attempt to relink its running Windows harness; moved complete discovery to the standalone ordinary-CI command and retained safe metadata/unit checks plus actual facade and current-harness regression discovery. No check was removed or weakened. Fresh full local CI, MSRV 1.88, production site build, four site unit tests and all thirteen browser checks passed after these fixes. No third review request is permitted. Merge/tag and registry approval remain operator-owned.

Hosted verification on 4eb1471 passed every tier except Windows performance: both full platform checks, all CodeQL analyses, advisory/licenses, six fuzz jobs, MSRV/neutral builds, TShark, site accessibility, native Windows integration and actual final package certification passed. [Run 34965333411 attempt 1](https://github.com/h8rt3rmin8r/fragcap/actions/runs/34965333411/attempts/1) retained a complete failed Windows performance campaign under synthetic PR merge revision 52ff39e07aeaebbf80b349051c09bfe615f9d7ed. `short.jsonl` SHA-256 is `17be4093086396283709e11106f81a99562dec441e03ad94828d6570d9ad47a4`. `quic-off` window 5 reached queue peak 4,096, dropped 305 application events and 167,038 payload bytes, and correctly reported one hard failure despite zero timing-breaching windows, successful forwarding, clean shutdown and conserved bytes. Other windows had zero queue loss. Root cause is not established.

Opened [#413](https://github.com/h8rt3rmin8r/fragcap/issues/413) in the official Project and ran one unchanged-job reproduction diagnostic. Attempt 2 completed and passed all fourteen cases, making hosted CI green on 4eb1471. Its report SHA-256 is `516afd746ca406aa4a5fa412b2596df8b15a26626b8784755fe0a18193a901ac`; artifact ID 10396111150. Both exact raw reports are [retained in this slice](evidence/README.md), and their copied bytes match the original downloaded digests. The official report validator rejects attempt 1 and accepts attempt 2 individually. The second campaign cannot erase or relabel the failed first campaign or satisfy the two-fresh-Windows-campaign agreement by ignoring a hard breach. No performance budget, queue bound, workload, retry policy or production behavior changed. Corrected the related performance documentation to distinguish bounded failure-detail suffix eviction from new-observation queue refusal. Post-record documentation default/net checks, notes and lint passed. S150 release verification and T017 remain pending #413 disposition; a production runtime investigation/fix is not silently folded into the approved documentation/readiness/release-preparation scope. The final record-only commit will receive its own hosted checks; this record does not invent their result.

## Non-claims and retained work

No tag, release creation, registry publication, installed sensitive product execution, real game, live sensitive packet recording, real host trust mutation, or fresh-start deletion was performed by the agent. Controlled mocks, fixtures, synthetic local protocol tests and builds are engineering verification only. Independent installed-Windows review remains not performed in the committed template. #331/#333/#334/#278/#372 stay open under their original acceptance authorities.

## Explicitly authorized #413 extension (2026-09-15)

The operator approved scoped production-runtime investigation and correction after #413 blocked verification. The next unchanged-code hosted campaign 34967816667 also failed, with `quic-off` windows 4/5 dropping 389/371 events and 222625/213671 bytes. Exact raw bytes and SHA-256 are retained in the evidence directory; no failed campaign is rewritten as acceptance. Research identifies avoidable byte-write/allocation amplification, not the precise historical host stall. Spec, clarify decision, quality checklist, plan/research/design/tasks, and blocking read-only analyze were extended before runtime implementation, covering 13 requirements, 8 success criteria and 22 tasks without constitutional conflict.

The controlled local baseline passed all fourteen cases. A new deterministic writer regression then failed against the original buffer: 4,096 metadata records required 373 writes. The correction uses finite 64 KiB serialization buffering and moves owned JSON maps, preserving the existing 64-event pending limit, exact record content/order, nonblocking 4,096-event admission, timeout/final flush and failure retirement. All twelve focused writer tests passed, including exact 64-write/output equivalence, mixed pending/queued QUIC and UDP storage failure, oversized direct-write failure, queue refusal, bounded identity loss, connection control retention and final correlation. Initial focused Clippy passed; final full-gate verification follows after the last regression addition.

The corrected local release short campaign passed all fourteen cases with zero hard failures; its exact dirty-source report remains a local measurement, not hosted fixed-source acceptance. Workload, registry digest, budgets, crypto, consent, artifact schema, sensitive-execution restriction and requested-review count remain unchanged. MSRV verification initially encountered a Windows lock because the ordinary CI process still held xtask.exe; rerun it after CI exits rather than treating a concurrent relink failure as a product defect. No visible child window occurred. Two fresh hosted fixed-code Windows campaigns and current-head final CI are still pending.

The extended full local `cargo xtask ci` completed with exit 0. After the final oversized-write regression, all twelve focused writer tests and final all-target/all-feature workspace Clippy passed. MSRV 1.88 workspace build passed after the CI executable exited. Default/net documentation checks, short notes, lint, both local canonical report validators, production export, four site unit tests and all thirteen browser tests passed. Strict UTF-8/no-BOM/LF integrity and whitespace checks passed for extension files. This is engineering evidence before push, not a prediction of hosted results.
