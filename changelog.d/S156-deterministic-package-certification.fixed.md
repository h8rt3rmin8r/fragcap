<!-- spec-impact: 25.2, 27.3, 28.1 -->
Windows package certification no longer fails nondeterministically when bounded socket polling misses a short-lived loopback connection. Report schema 4 records deterministic session evidence separately from socket diagnostics and gives failed predicates bounded public-safe identifiers.
