# Quickstart: Guided Reachability Calibration Front Door

## Focused Validation

```powershell
cargo test -p fragcap-cli --test cli_args --locked
cargo test -p fragcap-cli --test cli_help --locked
cargo test -p fragcap-cli --test cli_calibrate --locked
cargo test -p fragcap-cli --test cli_deep_capture --locked
```

Confirm the top-level surface, direct, Steam, publisher, warm, current-positive, non-positive fact, no-match, ambiguity, unavailable inventory, malformed topology, decline, wrong plan identifier, and controlled reachability outcomes. Parse every structured line and generated next command.

## Repository Gate

```powershell
cargo xtask ci
```

## Safety Inspection

- No process handle or prohibited instrumentation call is added.
- No automatic registration, trust, TLS, or protocol calibration path exists.
- No-effect states return before bundle or authority preparation.
- Reachability delegates to the existing S134-authorized path.
- Generated commands use durable identifiers.
- Issue #394 closes on merge while parent #380 remains open.
