[package]
name = "fragcap-http-mitm-proxy-audit"
version = "0.0.0"
edition = "2021"
publish = false
license = "Apache-2.0"

[workspace]

[dependencies]
http-mitm-proxy = { version = "=0.18.0", default-features = false, features = ["native-tls-client"] }
