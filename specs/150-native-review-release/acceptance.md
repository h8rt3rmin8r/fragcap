# S150 acceptance evidence

Date: 2026-09-15. Candidate: prepared v0.10.0 on release/0.10.0. This record reports controlled engineering evidence and release readiness, not independent #333 acceptance, operator installed-build validation, publication or final #334 completion.

## Chronological execution

1. Verified merged S149/source remote, created spec-kit specification, clarify/checklist, plan/research, tasks and cross-artifact analysis. Eleven requirements and six success criteria map to seventeen tasks without unresolved blocking ambiguity. Both requirement checklists passed before implementation.
2. Expanded parser-only current documentation corpus tests. The baseline failed on README's retired Deep Capture consent input and Mermaid node false positives. Added explicit Mermaid fence regression and corrected obsolete guidance. Default and net CLI-reference variants each passed all eight tests without command dispatch.
3. Added static twelve-area handoff tests before implementation. Baseline failed for missing committed registry and unimplemented negative validation. Final focused tests passed all six cases, including closed scope, source confinement, actual non-ignored test references and refusal of invented audit approval/provenance. Reused the existing threat-model test-source checker.
4. Corrected native architecture, first-session/calibration journey, compatibility applicability, CLI semantics and artifact authority. Added discoverable Doctor/troubleshooting, security/privacy, packaging/migration and stable API pages. The first manifest filter selected zero tests with the feature disabled; the corrected `cargo test -p fragcap --lib --features deep-capture --locked published_examples_match_the_versioned_reader` selected and passed the actual reader test. Published schema/examples are reused rather than inventing shortened artifacts.
5. Linked #411 into the official Project as S150/In progress, and posted bounded delivery references to #278/#331/#333. All parent independent/final criteria remain open. Doctor #372 remains operator reproduction on newly published bytes.
6. Release preflight caught unsupported historical `.feature.md` fragment suffixes from S130/S131/S132. Renamed them to `.added.md` with identical contents, retaining unknown-section refusal. Corrected S150's leading spec-impact comments. Documentation lint, specification check and changelog preview passed. Short-note validation correctly waited for the new release section.
7. Committed documentation preparation, previewed aggregate Cargo release without execution, then selected the structurally version-only subcommand because the aggregate banner still said Publishing with disabled publication. Previewed and executed `cargo release version 0.10.0 --workspace --config release.toml` (execute/no-confirm only on execution). Only local first-party manifest/lock versions changed. Offline workspace updates reconciled fuzz/performance locks without upgrading third-party packages.
8. Regenerated the existing three golden owners: facade goldens (7 tests), CLI Capture (24 tests), CLI extcap (19 tests), all passed. Updated exact embedded and portable conformance/staged-binary assertions, never historical physical measurements. Fresh graph snapshot retained package/edge counts 187/448 Linux, 186/424 Windows and 148/317 official release. Rebound the existing unsafe inventory only for first-party version metadata, without changing scope, expiry or exceptions.
9. The existing release task assembled 44 fragments into [0.10.0] dated 2026-09-15 and normalized historical Markdown wrapping. Consumed fragments are recoverable from Git history and represented in CHANGELOG.md. Its git-rm attempt could not stage the locally modified S150 decision file; targeted staging handles that deletion, not forced destructive removal. One-screen notes passed after assembly and do not copy the full changelog.
10. Initial full local CI passed Rust tests/clippy but rejected the stale unsafe graph digest. After the exact metadata reconciliation, the full gate was restarted. The declared MSRV 1.88 workspace build passed. Final aggregate evidence is recorded below when completion is observed.
11. Production site build and all four changelog unit tests passed. Browser tests initially rejected a stale fixed route count and TLS key-log search ranking. Route coverage now compares complete content inventory to exported routes, not a newly guessed count. All 69 routes passed 320/768/1440px sweeps, and current native guidance internal destinations/anchors passed. Added a bounded canonical key-log query pin to the authoritative output/sensitivity reference; focused search (including the new Library/Doctor pages) passed. Final full browser evidence is recorded below when completion is observed.

## Final local gates

`cargo xtask ci` completed with exit 0 after metadata reconciliation: fmt, clippy, workspace tests/doc-tests, lint, crate direction/license, closed supply-chain policy, package contract, wrappers/skills, default/net documentation checks, specification currency, guided calibration acceptance, threat model, review readiness, fuzz/failure inventories, portable HTTP/TLS conformance, performance authority and retained Windows evidence all passed. The declared MSRV 1.88 build completed with exit 0. TShark and actual final-package installer execution remain distinct hosted CI tiers; static local contract success does not relabel them as local installed-build proof. The final production build passed, all four changelog unit tests passed, and all thirteen browser tests passed (69 complete exported routes, three viewports, named diagrams, contrast, retired/current searches, native guide links/anchors and not-found recovery). Post-final-edit documentation default/net checks, notes and lint also completed with exit 0. Strict UTF-8/no-BOM and mojibake sanity checks plus git diff whitespace checks passed.

## External review and delivery

PR publication, current-head CI and bot-review disposition remain pending. At most one manual second-round Codex request is allowed. Merge/tag and registry approval remain operator-owned.

## Non-claims and retained work

No tag, release creation, registry publication, installed sensitive product execution, real game, live sensitive packet recording, real host trust mutation, or fresh-start deletion was performed by the agent. Controlled mocks, fixtures, synthetic local protocol tests and builds are engineering verification only. Independent installed-Windows review remains not performed in the committed template. #331/#333/#334/#278/#372 stay open under their original acceptance authorities.
