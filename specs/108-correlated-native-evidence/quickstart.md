# Quickstart: Correlated Native Evidence

1. Run correlation tests for interval overlap, reuse, retention, permutations, IPv4/IPv6, multiplexing, and no controlled fabrication.
2. Verify ordered application correlation records and accounting before the trailer.
3. Run HAR conformance cases for complete, partial, binary, truncated, failed, and interrupted transactions.
4. Run manifest v1/v2, schema drift, path, contradiction, prefix, and fault-injection tests.
5. Run controlled Deep Capture and inspect the application, HAR, and final manifest artifacts.
6. Run `cargo xtask ci`, `cargo xtask msrv`, and dependency policy checks.
