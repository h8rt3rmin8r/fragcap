<!-- spec-impact: 15, 17.2.1, 25, 28.1 -->
`fragcap calibrate` now advances through a bounded sequence of useful reachability and protocol checks in one invocation. Every attempt still displays and requires its own current authorization plan, writes to a distinct bundle, and advances only after fresh exact evidence proves that case succeeded.
